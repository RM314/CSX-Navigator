Minimal file split for the CSX Navigator React demo.

Suggested install:
1. npm create vite@latest csx-ui -- --template react-ts
2. copy the contents of this folder into that project
3. npm install
4. npm install tailwindcss @tailwindcss/vite
5. npm install -D @vitejs/plugin-react
6. npm run dev
